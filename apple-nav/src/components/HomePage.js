import React from "react";

const HomePage = () => {
  return (
    <>
      <h1>Welcome to Homepage!</h1>
      <p>
        We can use this HomePage component as an homepage content section
        because it will only be rendered when none of the menu links are
        selected.
      </p>
    </>
  );
};

export default HomePage;
